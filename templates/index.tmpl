<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>MQTT SIMULATOR CONFIGURATION</title>
</head>
<body>
    <div>
        <form>
            <div>
                <textarea id="textarea" style="height: 350px; width: 800px"></textarea>
            </div>
            <div>
                <button type="submit">save</button>
            </div>
        </form>

    </div>


    <script>
        // document.getElementById("textarea").value = JSON.stringify("{{.template}}", undefined, 2)
        document.getElementById("textarea").value = "{{.template}}"
    </script>
</body>
</html>